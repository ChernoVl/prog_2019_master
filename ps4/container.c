#include "container.h"
#include <stdlib.h>

struct container* create_container(struct container* first, enum container_type type, void* entry)
{
	if (entry && first)
	{
		return NULL;
	}
	return NULL;
}